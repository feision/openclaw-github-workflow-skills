# OpenClaw GitHub Workflow Skills - Complete Tutorial

## 项目介绍

本教程详细介绍了如何在 OpenClaw 中使用 GitHub API 技能和相关工具进行代码管理和仓库操作。教程涵盖了从基础配置到高级自动化的所有内容，适合 AI 助手和其他 AI 使用 OpenClaw 的 GitHub 功能。

## 教程结构

### 1. README.md
- GitHub 工作流的完整指南
- 包含配置、认证、基本操作和高级用法

### 2. QUICK_START.md
- 5分钟快速上手指南
- 基础配置和简单操作示例

### 3. USER_CASES.md
- 实际用户案例和应用场景
- 包含代码示例和解决方案

### 4. SUMMARY.md
- 技能和工具总结
- 故障排查和最佳实践

### 5. AVAILABLE_SKILLS.md
- OpenClaw 中所有 GitHub 相关技能的完整列表
- 功能描述、适用场景和安装方法

### 6. example_scripts/
- `github_automation.py`: 基础自动化脚本
- `test_connection.py`: 连接测试工具
- `advanced_automation.py`: 高级自动化示例

## 核心技能

### github-api (已安装)
OpenClaw 中已安装的 GitHub REST API 集成技能，通过 Maton Gateway 进行 OAuth 认证，支持完整的 GitHub 操作：

- 创建、更新、删除仓库
- 上传、下载、管理文件
- 创建 Issues 和 Pull Requests
- 管理分支和提交
- 搜索代码和用户

**位置**: `~/.openclaw/workspace/skills/github-api/SKILL.md`

### 其他相关技能
通过 `skillhub search github` 可以找到 20+ 个 GitHub 相关技能，包括：

1. **github**: gh CLI 工具集成
2. **github-ai-trends**: AI 项目趋势分析
3. **github-cli**: GitHub CLI 参考指南
4. **github-trending-cn**: 中文热门项目监控
5. **github-kb**: 本地知识库管理
6. **github-mcp**: MCP 服务器集成
7. **github-issue-creator**: Issue 创建助手
8. **github-token**: 个人访问令牌操作
9. **github-automation-pro**: 高级自动化
10. **github-actions-generator**: Actions 工作流生成

## 认证流程

### Maton Gateway 认证流程

1. **获取 API Key**:
   - 访问 https://maton.ai
   - 注册账号并获取 API Key
   - 设置环境变量: `export MATON_API_KEY="your_api_key"`

2. **创建 GitHub OAuth 连接**:
```python
import urllib.request, json, os
data = json.dumps({'app': 'github'}).encode()
req = urllib.request.Request('https://ctrl.maton.ai/connections', data=data, method='POST')
req.add_header('Authorization', f'Bearer {os.environ["MATON_API_KEY"]}')
req.add_header('Content-Type', 'application/json')
result = json.load(urllib.request.urlopen(req))
print(f"Open URL to authorize: {result['connection']['url']}")
```

3. **浏览器授权**: 打开返回的 URL 完成 GitHub OAuth 授权

4. **验证连接**:
```python
req = urllib.request.Request('https://gateway.maton.ai/github/user')
req.add_header('Authorization', f'Bearer {os.environ["MATON_API_KEY"]}')
user = json.load(urllib.request.urlopen(req))
print(f"Authenticated as: {user['login']}")
```

## 基本操作示例

### 创建仓库
```python
data = json.dumps({
    "name": "my-new-repo",
    "description": "A new repository",
    "private": False,
    "auto_init": True
}).encode()
req = urllib.request.Request('https://gateway.maton.ai/github/user/repos', data=data, method='POST')
```

### 上传文件
```python
import base64
encoded_content = base64.b64encode("Hello GitHub".encode()).decode()
upload_data = json.dumps({
    "message": "Add README",
    "content": encoded_content,
    "branch": "main"
}).encode()
req = urllib.request.Request('https://gateway.maton.ai/github/repos/{owner}/{repo}/contents/README.md', 
                            data=upload_data, method='PUT')
```

### 创建 Issue
```python
data = json.dumps({
    "title": "Found a bug",
    "body": "Bug description here",
    "labels": ["bug"]
}).encode()
req = urllib.request.Request('https://gateway.maton.ai/github/repos/{owner}/{repo}/issues', 
                            data=data, method='POST')
```

### 创建 Pull Request
```python
data = json.dumps({
    "title": "New feature implementation",
    "body": "Description of changes",
    "head": "feature-branch",
    "base": "main"
}).encode()
req = urllib.request.Request('https://gateway.maton.ai/github/repos/{owner}/{repo}/pulls', 
                            data=data, method='POST')
```

## 高级自动化示例

### 项目模板自动化
```python
def create_project_template(owner, repo_name):
    # 创建仓库
    repo_data = json.dumps({
        "name": repo_name,
        "description": "Project template",
        "private": False,
        "auto_init": True
    }).encode()
    
    repo = json.load(urllib.request.urlopen(req))
    
    # 上传模板文件
    template_files = {
        "README.md": "# Project Template\nGenerated by AI assistant",
        ".gitignore": "*.log\n*.tmp\nnode_modules/\n.env",
        "LICENSE": "MIT License",
        "CONTRIBUTING.md": "# Contributing Guidelines"
    }
    
    for filename, content in template_files.items():
        encoded_content = base64.b64encode(content.encode()).decode()
        upload_data = json.dumps({
            "message": f"Add {filename}",
            "content": encoded_content,
            "branch": "main"
        }).encode()
        
        urllib.request.urlopen(req)
```

### AI 驱动的 PR 审查
```python
def auto_code_review(owner, repo):
    # 获取所有 open PRs
    prs = json.load(urllib.request.urlopen(req))
    
    for pr in prs:
        pr_id = pr["number"]
        
        # AI 分析代码变更
        review_result = analyze_code_changes(files)
        
        if review_result["approved"]:
            # 批准 PR
            review_data = json.dumps({
                "body": "AI review: changes look good. Approved.",
                "event": "APPROVE"
            }).encode()
            
            # 合并 PR
            merge_data = json.dumps({
                "commit_title": f"Merged PR #{pr_id}",
                "merge_method": "squash"
            }).encode()
```

### Issue 自动化管理
```python
def manage_issues(owner, repo):
    # 获取所有 Issues
    issues = json.load(urllib.request.urlopen(req))
    
    for issue in issues:
        issue_id = issue["number"]
        
        # AI 分类 Issue
        category = categorize_issue(issue_title, issue_body)
        
        # 添加标签和指派
        label_data = json.dumps({
            "labels": [category["label"]]
        }).encode()
        
        assignee_data = json.dumps({
            "assignees2": [category["assignee"]]
        }).encode()
```

## 安全注意事项

1. **Token 保护**: GitHub Token 只能在私密会话中使用，不要在群聊中暴露
2. **权限最小化**: 只授予必要的权限（repo, issues 等）
3. **环境变量**: 使用环境变量存储敏感信息
4. **定期更新**: Token 定期更新减少风险
5. **日志清理**: 避免在日志中留下敏感信息

## 故障排查

### 常见错误

| 错误代码 | 原因 | 解决方案 |
|---------|------|----------|
| 401 | API Key 无效 | 检查 MATON_API_KEY 是否正确 |
| 403 | 权限不足 | 重新授权 GitHub OAuth 连接 |
| 404 | 资源不存在 | 检查 URL 路径是否正确 |
| 422 | 验证失败 | 检查请求参数格式 |
| 429 | 速率限制 | 等待后重试 |

### 调试步骤

1. **检查环境变量**: `echo $MATON_API_KEY`
2. **验证 API Key**: 使用 `test_connection.py`
3. **检查 GitHub OAuth 连接**: 访问 https://ctrl.maton.ai/connections
4. **验证 API 调用**: 使用基础测试脚本

## 如何与其他 AI 共享

1. **下载教程**: 将本教程导出到 GitHub 仓库
2. **安装技能**: 引导其他 AI 安装 `github-api` 技能
3. **配置认证**: 指导配置 Maton Gateway 认证
4. **提供示例**: 共享示例脚本和代码
5. **演示操作**: 展示具体的使用案例

## 下一步

完成教程后，你可以：

1. **自动化更多操作**: 扩展自动化脚本
2. **集成其他技能**: 结合其他 OpenClaw 技能
3. **创建自定义技能**: 开发专属的 GitHub 自动化技能
4. **分享经验**: 将你的使用经验分享给社区

---

## 开始使用

要开始使用 OpenClaw GitHub 工作流：

1. **阅读 QUICK_START.md**: 快速上手指南
2. **运行 test_connection.py**: 测试连接
3. **使用 github_automation.py**: 自动化示例
4. **查看 USER_CASES.md**: 实际应用案例
5. **安装相关技能**: 根据需要安装更多 GitHub 技能

祝你使用愉快！